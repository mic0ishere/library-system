version: "3.8"
services:
  db:
    image: mysql:latest
    environment:
      MYSQL_DATABASE: 'database'
      MYSQL_USER: 'librarysystem'
      MYSQL_PASSWORD: 'some-secret-password'
    ports:
      - "3306:3306"